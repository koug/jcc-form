Template.inputText.helpers({
//	showLabel: function(field) {
//        var index = field.replace(/.*\.(\d+)$/, "$1");
//        if (index === field || index === "0") {
//            return true;
//        }
//        return false;
//    }
    showSchema: function(schema) {
        console.log(schema);
    }
});